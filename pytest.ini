[pytest]
asyncio_mode = auto
markers =
    network: test depends on a live Together.ai / Ollama call; skipped by default. Use `pytest --run-network` to include.
    slow: crash-injection and boot-reconciliation tests (P0.5); run with default pytest (no flag needed).
